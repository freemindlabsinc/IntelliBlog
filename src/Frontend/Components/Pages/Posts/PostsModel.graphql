query PostsModel($maxRows: Int=10) {
  posts:posts(first: $maxRows) {
    nodes {
      ... PostCardData    
      
      sources: sources {
        nodes {         
            ... SourceCardData
        }
        totalCount
      }            
    }
    totalCount
  }
}
