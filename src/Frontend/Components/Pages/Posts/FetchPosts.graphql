query FetchPosts($maxRows: Int=10) {
  posts:posts(first: $maxRows) {
    nodes {
      ... PostCardData    
      
      sources: sources {
        nodes {         
            ... SourceCardData
        }
      }            
    }
  }
}

fragment SourceCardData on Source {
    id      
    name
    url
    type
}