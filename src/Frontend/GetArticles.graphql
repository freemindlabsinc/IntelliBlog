query GetData($maxRows: Int=10) {
  posts(first: $maxRows) {
    nodes {
      id      
      title
      description
      sources {
        id
        linkedOn
        postId
      }      
      text
    }
  }
}