query GetData($maxRows: Int=10) {
  articles(first: $maxRows) {
    nodes {
      id      
      title
      description
      sources {
        id
        linkedOn
        articleId
      }
      comments {
        text
      }
      text
    }
  }
}